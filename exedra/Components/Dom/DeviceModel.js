
export class Device {
    constructor(id, name, address, coords, failures, configuration, metering) {
      this.id = id;
      this.name = name;
      this.address = address;
      this.coords = coords;
      this.failures = failures;
      this.configuration = configuration;
      this.metering = metering
    }
  }
  
//   export default Device;